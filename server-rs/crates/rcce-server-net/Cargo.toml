[package]
name = "rcce-server-net"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "RCCE2 server-side ENet HOST: listen on a port, accept N client peers, poll events, send packets. Reuses the vendored RCCE2 ENet fork (wire-compatible with the live clients)."

[dependencies]
enet-sys.workspace = true

[lints]
workspace = true
