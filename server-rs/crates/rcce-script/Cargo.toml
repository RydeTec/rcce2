[package]
name = "rcce-script"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "RSL (RealmCrafter Scripting Language) interpreter — a Rust replacement for the closed-source BriskVM the Blitz server embeds. Lexer/parser/tree-walking interpreter + the BVM_* command surface."

[dependencies]
